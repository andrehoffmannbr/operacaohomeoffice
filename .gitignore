.DS_Store
Thumbs.db
node_modules/
.vercel
.env
*.log

# Master local do antes/depois — usado só pra gerar os WebP responsivos
# (perfil-depois-480.webp / perfil-depois-320.webp). Não é servido pela
# página e não deve voltar ao repositório num "git add .".
assets/images/depois.png
